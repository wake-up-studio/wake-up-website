<?php
    $item = $data["user"];
    $date = $item -> getCreatedAt();
?>

<!doctype html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title><?= $item -> getEmail() ?></title>
</head>
<body>
    <h1><?= $item -> getFirstName() ?> <?= $item -> getLastName() ?></h1>
    <h2><?= $item -> getId() ?></h2>
    <p><?= $item -> getEmail() ?></p>
    <p><?= $item -> getRole() ?></p>
    <p><?= $date -> format("Y-m-d H:i:s") ?></p>
    <button><a href="?route=updateUser&user_id=<?= $item -> getId() ?>">Modifier</a></button>
    <button><a href="?route=deleteUser&user_id=<?= $item -> getId() ?>">Supprimer</a></button>
</body>
</html>